repos:
# Use ruff for linting and formatting
- repo: https://github.com/astral-sh/ruff-pre-commit
  rev: v0.4.6
  hooks:
    # Linter
    - id: ruff
      args: [ --fix ]
    # Formatter
    - id: ruff-format
