[project]
name = "shodo-smolvla"
version = "0.1.0"
requires-python = ">=3.13,<3.14"
dependencies = ["shodo-arm", "lerobot[smolvla,peft]==0.6.1"]

[tool.uv]
package = false

[tool.uv.sources]
shodo-arm = { path = "../..", editable = true }
