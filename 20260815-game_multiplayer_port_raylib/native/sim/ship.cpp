#include "battle.h"
// Runtime ship construction lives in Battle so the public model remains small. This translation
// unit is deliberately retained as the seam for a future standalone ship factory.
